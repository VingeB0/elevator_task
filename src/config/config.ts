export const ANIMATION_TIME: number = 0.6; //milliseconds

export const buildingsSize: { WIDTH: number; HEIGHT: number } = {
  WIDTH: 50, //less than 30 is not recommended
  HEIGHT: 50, //less than 30 is not recommended
};

export const DEFAULT_ELEVATORS: number = 3;
export const DEFAULT_FLOORS: number = 10;
